# Production cutover — 2026-09-26

Status: provisional until live-market validation. Paper trading only.

- Local scheduled task disabled; startup/supervisor refuse to run while
  `.runtime/cloud-writer.lock` exists. Local database retained read-only as rollback evidence.
- Final stopped-source snapshot imported into a fresh production PostgreSQL database:
  8 main positions, 5 Shadow positions, 12 fills, 9 monitor cursors; exact table comparison passed.
  Three legacy adoptions remain explicitly identified, without fabricated history.
- Cloud is the sole writer. All roles and PostgreSQL restarted; position/fill digests
  remained identical, accounting validation passed, duplicate event keys absent.
- Telegram existing status-message references preserved separately, avoiding new cards.
  News publication boundary prevents replaying pre-cutover news. Operational topic references
  and watchlist persist in PostgreSQL but are not in the minimal Recovery-State artifact;
  a disaster restore must restore/reconcile those separately before enabling Telegram.
- Encrypted GitHub recovery artifact restored into a fresh database with exact row comparison;
  no news history or outbox replayed. Keep the private decryption identity offline.
- Public API: https://2-28-100-77.sslip.io; Pages BACKEND_URL updated.
- OpenRouter Luna remains configurable independently per task. $20 monthly guardrail,
  automatic top-up disabled. AI requests paced across roles (3.2 seconds by default)
  for the provider's 20 RPM new-account quota. Pacing sleeps outside transactions;
  the independent Position Monitor never waits for AI.
- GitHub read-only market smoke runs on weekdays at 13:40 and 14:40 UTC, checking
  market state, fresh quotes, monitor progress and accounting. Actual TP/SL evidence
  requires natural live-market events; a closed-market pass is not live TP/SL proof.

## Rollback safety

Never remove the local writer lock and start the old database after cloud activity.
Stop all cloud writers first, export current active state, validate reverse import and
reconcile Telegram publication boundaries before selecting exactly one writer environment.
Do not delete local rollback evidence yet.
